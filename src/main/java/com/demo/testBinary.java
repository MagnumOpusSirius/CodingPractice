package com.demo;

public class testBinary {
    /*
    //find pivot first:
    - mid => start= 0 and end = last array index
    - if value at mid-1 > greater return that value as the pivot.
    - else if: val at mid+1 < mid => set mid as the pivot
    - else: if the value at start > mid => consider the left half. else consider the right half.

    -divide the array into 2 sub array. from the start to pivot. And Pivot+1 to end.
     */
}
